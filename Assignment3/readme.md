# Assignment 3

This folder holds the Assignment 3 implementation:

- `FunctionApp/` contains the Azure Function that listens to Service Bus and writes incoming messages to Azure Storage.
- `terraform/` contains the infrastructure for the Function App, Storage Account, Service Bus, queue, and identity-based access.

The repository-level workflows live in `.github/workflows/`.